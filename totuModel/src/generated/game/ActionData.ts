// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.0.2
//   protoc               v3.21.12
// source: game/ActionData.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { PlayCard } from "./PlayCard";

export const protobufPackage = "space.junodev.model.game";

export interface ActionData {
  playCard?: PlayCard | undefined;
}

function createBaseActionData(): ActionData {
  return { playCard: undefined };
}

export const ActionData = {
  encode(message: ActionData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.playCard !== undefined) {
      PlayCard.encode(message.playCard, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ActionData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActionData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.playCard = PlayCard.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ActionData {
    return { playCard: isSet(object.playCard) ? PlayCard.fromJSON(object.playCard) : undefined };
  },

  toJSON(message: ActionData): unknown {
    const obj: any = {};
    if (message.playCard !== undefined) {
      obj.playCard = PlayCard.toJSON(message.playCard);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ActionData>, I>>(base?: I): ActionData {
    return ActionData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ActionData>, I>>(object: I): ActionData {
    const message = createBaseActionData();
    message.playCard = (object.playCard !== undefined && object.playCard !== null)
      ? PlayCard.fromPartial(object.playCard)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
